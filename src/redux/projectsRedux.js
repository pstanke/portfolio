/* SELECTORS */
export const getProjects = (state) => state.projects;
/* REDUCER */
export const projectsReducer = (statePart = [], action) => {
  switch (action.type) {
    default:
      return statePart;
  }
};
